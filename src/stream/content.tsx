import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';
import { Node } from 'slate';


// const contentChange$ = (content$: Observable<Node[]>) => content$.pipe(
//   map(content$ => )
// );
